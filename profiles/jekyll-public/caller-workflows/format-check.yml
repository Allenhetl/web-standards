# Caller stub — real logic lives in Allenhetl/web-standards.
name: Format check
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  prettier:
    permissions:
      contents: read
      pull-requests: write # lets the reusable workflow post a PR comment on failure
    uses: Allenhetl/web-standards/.github/workflows/format-check.yml@v1
